import { Routes } from '@angular/router';
import { SimControls } from './sim-controls';

export const SIM_CONTROL_ROUTES: Routes = [
    {
      path: 'sim-controls',
      component: SimControls
    }
];